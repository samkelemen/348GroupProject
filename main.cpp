/*
 *
*/

#include "user_interface.h"
//#include "parser.h"
//#include "calculator.h"

using  std::string;


int main() {
    while (true) {
        string input = input();
        // class compute_graph = parser(string);
        // float result = calculator(compute_graph);
        // output_result(result);
    }
}